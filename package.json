{
  "scripts": {
    "start": "parcel serve -d public src/index.pug",
    "build": "parcel build -d public --no-source-maps --experimental-scope-hoisting src/index.pug"
  },
  "devDependencies": {
    "glslify-bundle": "^5.1.1",
    "glslify-deps": "^1.3.1",
    "parcel-bundler": "^1.12.4",
    "pug": "^2.0.4",
    "typescript": "^3.7.5"
  },
  "dependencies": {
    "gl-matrix": "^3.2.1"
  },
  "browserslist": [
    "last 1 version",
    "not ie > 0",
    "not dead"
  ]
}
